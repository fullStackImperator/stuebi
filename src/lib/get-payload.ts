/**
 * @deprecated Use `getPayloadLocal` from `@/lib/payload-local-api` for clarity.
 * Same behavior: Local API singleton for server-only code.
 */
export { getPayloadLocal as getPayloadClient } from "@/lib/payload-local-api";
